var cmpstmt = {
	type: 'cmpstmt',
	val: ['array of statement object']
}

var expstmt = {
	type: 'expstmt',
	val: ['instruction parts']
}

var ifstmt = {
	type: 'ifstmt',
	val: {
		'exp': ,
		'if': ,
		'else':
	}
}

var whilestmt = {
	type: 'whilestmt',
	val: {
		'exp': ,
		'body':
	}
}

var jumpstmt = {
	type: 'jmpstmt',
	val: ['instruction parts']
}

var decstmt = {
	type: 'decstmt',
	val: ['instruction parts']
}